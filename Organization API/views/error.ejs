<!-- Path: /views/error.ejs -->
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Error' %> - Organization Management System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    <div class="min-h-screen flex items-center justify-center px-4">
        <div class="max-w-lg w-full text-center">
            <div class="bg-white shadow-lg rounded-lg p-8">
                <div class="text-6xl text-red-500 mb-4">
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
                
                <h1 class="text-3xl font-bold text-gray-900 mb-2">
                    <%= typeof statusCode !== 'undefined' ? statusCode : '500' %>
                </h1>
                
                <h2 class="text-xl font-semibold text-gray-700 mb-4">
                    <%= typeof title !== 'undefined' ? title : 'Error' %>
                </h2>
                
                <p class="text-gray-600 mb-8">
                    <%= typeof message !== 'undefined' ? message : 'An unexpected error occurred' %>
                </p>
                
                <% if (typeof error !== 'undefined' && error && process.env.NODE_ENV === 'development') { %>
                    <div class="bg-gray-100 rounded p-4 mb-6 text-left">
                        <h3 class="font-semibold text-gray-700 mb-2">Error Details:</h3>
                        <pre class="text-xs text-gray-600 overflow-x-auto"><%= error.stack || error %></pre>
                    </div>
                <% } %>
                
                <div class="flex justify-center space-x-4">
                    <a href="/" class="inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition">
                        <i class="fas fa-home mr-2"></i>
                        Go to Dashboard
                    </a>
                    
                    <button onclick="history.back()" class="inline-flex items-center px-4 py-2 bg-gray-300 text-gray-700 rounded hover:bg-gray-400 transition">
                        <i class="fas fa-arrow-left mr-2"></i>
                        Go Back
                    </button>
                </div>
            </div>
        </div>
    </div>
</body>
</html>